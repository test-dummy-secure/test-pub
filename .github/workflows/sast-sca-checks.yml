# .github/workflows/sast-sca-checks.yml

name: Security Checks (SAST + SCA)

on:
  pull_request:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:

  # ================================================================
  # SAST
  # ================================================================
  sast:
    name: SAST (code check)
    runs-on: ubuntu-latest

    container: semgrep/semgrep

    continue-on-error: ${{ vars.REPORT_ONLY == 'true' }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ------------------------------------------------------------
      # Run Semgrep
      # ------------------------------------------------------------
      - name: Run Semgrep SAST scan
        shell: sh
        run: |
          echo "========================================"
          echo "Running SAST scan"
          echo "========================================"

          if [ "${{ github.event_name }}" = "pull_request" ]; then

            semgrep scan \
              --config p/owasp-top-ten \
              --baseline-commit "${{ github.event.pull_request.base.sha }}" \
              --severity ERROR \
              --json \
              --output sast-report.json \
              --error

          else

            semgrep scan \
              --config p/owasp-top-ten \
              --config p/security-audit \
              --json \
              --output sast-report.json || true

          fi

          echo ""
          echo "SAST report generated."

          ls -lh sast-report.json

      # ------------------------------------------------------------
      # Save SAST report in GitHub
      # ------------------------------------------------------------
      - name: Keep SAST report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sast-report-${{ github.run_id }}
          path: sast-report.json

      # ------------------------------------------------------------
      # Upload SAST to DefectDojo
      # ------------------------------------------------------------
      - name: Send SAST report to DefectDojo
        if: >
          always() &&
          vars.SEND_TO_DEFECTDOJO == 'true'
        shell: sh
        run: |
          echo "========================================"
          echo "Uploading SAST report to DefectDojo"
          echo "========================================"

          echo "DefectDojo URL:"
          echo "${{ secrets.DEFECTDOJO_URL }}"

          echo "Repository:"
          echo "${{ github.repository }}"

          echo "Product Type:"
          echo "GitHub Repositories"

          echo "Engagement:"
          echo "SAST"

          curl --fail-with-body \
            -X POST \
            "${{ secrets.DEFECTDOJO_URL }}/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
            -F "scan_type=Semgrep JSON Report" \
            -F "file=@sast-report.json" \
            -F "product_name=${{ github.repository }}" \
            -F "product_type_name=GitHub Repositories" \
            -F "engagement_name=SAST" \
            -F "auto_create_context=true" \
            -F "close_old_findings=true"

          echo ""
          echo "SAST report successfully uploaded to DefectDojo."


  # ================================================================
  # SCA
  # ================================================================
  sca:
    name: SCA (dependency check)
    runs-on: ubuntu-latest

    continue-on-error: ${{ vars.REPORT_ONLY == 'true' }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      # ------------------------------------------------------------
      # Check whether repository contains dependency files
      # ------------------------------------------------------------
      - name: Check for dependency manifests
        id: dependency_check
        shell: bash
        run: |

          echo "========================================"
          echo "Checking dependency manifests"
          echo "========================================"

          FOUND=0

          for file in \
            package.json \
            package-lock.json \
            npm-shrinkwrap.json \
            yarn.lock \
            pnpm-lock.yaml \
            requirements.txt \
            Pipfile.lock \
            poetry.lock \
            pyproject.toml \
            go.mod \
            go.sum \
            pom.xml \
            build.gradle \
            build.gradle.kts \
            composer.lock \
            Gemfile.lock \
            Cargo.lock
          do

            if [ -f "$file" ]; then
              echo "Found dependency file: $file"
              FOUND=1
            fi

          done

          if [ "$FOUND" -eq 1 ]; then

            echo ""
            echo "Dependency files found."
            echo "SCA scan will run."

            echo "has_dependencies=true" >> "$GITHUB_OUTPUT"

          else

            echo ""
            echo "No supported dependency manifests found."
            echo "SCA scan will be skipped."

            echo "has_dependencies=false" >> "$GITHUB_OUTPUT"

          fi

      # ------------------------------------------------------------
      # Download OSV Scanner
      # ------------------------------------------------------------
      - name: Download OSV-Scanner
        if: steps.dependency_check.outputs.has_dependencies == 'true'
        shell: bash
        run: |

          echo "Downloading OSV-Scanner..."

          curl -sSL \
            https://github.com/google/osv-scanner/releases/latest/download/osv-scanner_linux_amd64 \
            -o osv-scanner

          chmod +x osv-scanner

          ./osv-scanner --version

      # ------------------------------------------------------------
      # Run SCA
      # ------------------------------------------------------------
      - name: Run OSV dependency scan
        if: steps.dependency_check.outputs.has_dependencies == 'true'
        shell: bash
        run: |

          echo "========================================"
          echo "Running SCA dependency scan"
          echo "========================================"

          ./osv-scanner scan -r . \
            --format json \
            --output sca-report.json

          echo ""
          echo "SCA scan completed."

          if [ ! -f sca-report.json ]; then
            echo "ERROR: sca-report.json was not generated."
            exit 1
          fi

          ls -lh sca-report.json

      # ------------------------------------------------------------
      # Check serious vulnerabilities
      # ------------------------------------------------------------
      - name: Check for serious fixable vulnerabilities
        if: steps.dependency_check.outputs.has_dependencies == 'true'
        shell: bash
        run: |

          if [ ! -f sca-report.json ]; then
            echo "ERROR: sca-report.json was not generated."
            exit 1
          fi

          COUNT=$(jq '
            [
              .results[]?.packages[]?.vulnerabilities[]?
              |
              select(
                (.database_specific.severity == "HIGH")
                or
                (.database_specific.severity == "CRITICAL")
              )
              |
              select(
                [
                  .affected[]?.ranges[]?.events[]?
                  | has("fixed")
                ] | any
              )
            ] | length
          ' sca-report.json 2>/dev/null || echo 0)

          echo "Fixable HIGH/CRITICAL dependency vulnerabilities: $COUNT"

          if [ "$COUNT" -gt 0 ]; then
            echo "HIGH/CRITICAL fixable vulnerabilities found."

            if [ "${{ vars.REPORT_ONLY }}" = "true" ]; then
              echo "REPORT_ONLY=true -> not blocking pipeline."
            else
              exit 1
            fi
          fi

          echo "SCA vulnerability check completed."

      # ------------------------------------------------------------
      # Save SCA report
      # ------------------------------------------------------------
      - name: Keep SCA report
        if: always() && steps.dependency_check.outputs.has_dependencies == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sca-report-${{ github.run_id }}
          path: sca-report.json

      # ------------------------------------------------------------
      # Upload SCA to DefectDojo
      # ------------------------------------------------------------
      - name: Send SCA report to DefectDojo
        if: >
          always() &&
          steps.dependency_check.outputs.has_dependencies == 'true' &&
          vars.SEND_TO_DEFECTDOJO == 'true'
        shell: bash
        run: |

          echo "========================================"
          echo "Uploading SCA report to DefectDojo"
          echo "========================================"

          echo "DefectDojo URL:"
          echo "${{ secrets.DEFECTDOJO_URL }}"

          echo "Repository:"
          echo "${{ github.repository }}"

          echo "Product Type:"
          echo "GitHub Repositories"

          echo "Engagement:"
          echo "SCA"

          curl --fail-with-body \
            -X POST \
            "${{ secrets.DEFECTDOJO_URL }}/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
            -F "scan_type=OSV Scan" \
            -F "file=@sca-report.json" \
            -F "product_name=${{ github.repository }}" \
            -F "product_type_name=GitHub Repositories" \
            -F "engagement_name=SCA" \
            -F "auto_create_context=true" \
            -F "close_old_findings=true"

          echo ""
          echo "SCA report successfully uploaded to DefectDojo."
